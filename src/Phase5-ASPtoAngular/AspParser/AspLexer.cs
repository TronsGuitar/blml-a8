using System;

namespace BLML.Phase5ASPtoAngular.AspParser
{
    public class AspLexer
    {
        /* TODO: Implementation Logic
         * 1. Tokenize Classic ASP files (.asp).
         * 2. Distinguish between HTML content and Script blocks (<% ... %>).
         * 3. Handle server-side includes (SSI) <!-- #include file="..." -->.
         * 4. Recognize script directives like @Language and CodePage.
         * 5. Pass script tokens to the VBScript/JScript lexer component.
         */
        public AspLexer()
        {
        }
    }
}
